setx DOCKER_BUILDKIT 0

each commit has hash github.sha can be used as tag while pushing the version to docker

github.base_ref  (to branch)(sourch branch)
github.ref_name  (from branch)(target branch)
